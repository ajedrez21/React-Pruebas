const activo = false;

//OPERADOR TERNARIO
const mensaje = (activo) ? 'Activo' : 'Inactivo';

console.log(mensaje);

//CON CONDICION AND PARA PODER HACER UN IF MAS RAPIDO
const M1 = !activo && 'Activo';

console.log(M1);